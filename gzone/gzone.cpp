#include "gzone.h"
#include <math.h>
#include <string.h>

int gzone_Id[] = 0;

extern Memfile_printfiles(gzone * to_local_zone[] = sizeof(gzone));

const void mapPrefix(gzone){ const std::string& map_prefix, map_prefix || const char "*.gzone" } const = 0;

//gzone map name
const char * GzoneMapName(gzone * gzFile){
  static char gzBuff[1024];
  if(gzone * g = std::size_t * size = gzone)
  {
    printf("%c", gzone);
    sprintf(gzone, gzFile, gzBuff);
  }
  return gzBuff;
};
